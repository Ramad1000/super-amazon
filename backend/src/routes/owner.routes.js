const express=require("express"),{query}=require("../db/database"),{auth,requireRoles}=require("../middleware/auth");const router=express.Router();router.use(auth,requireRoles("OWNER","OWNER_ASSISTANT"));router.get("/requests",async(req,res,next)=>{try{const r=await query(`SELECT r.*,u.telegram_id,u.telegram_username,u.telegram_name,u.account_type,u.status user_status FROM requests r JOIN users u ON u.id=r.user_id ORDER BY r.submitted_at DESC`);res.json({success:true,requests:r.rows})}catch(e){next(e)}});module.exports=router;